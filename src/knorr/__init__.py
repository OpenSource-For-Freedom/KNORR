"""Knörr -- Malicious Container Intelligence Engine.

The container sibling to git_warden (repos) and git_paca (packages): discovers,
STATICALLY analyzes, and catalogs malicious container images. Static analysis
only in the MVP -- images are pulled and read, never executed.
"""

__version__ = "0.0.1"
